# Feature: contact-conversion

Module ID: `contact-conversion` (see `CAPABILITY-MAP.md`). Depends on `studio-page`. Runs in parallel with `media-interactions`; both feed `production-delivery`.

## Objective

Turn the static contact section into a working fallback conversion path: a validated, spam-protected form that delivers the enquiry by email from the server, with clear success and error states, next to the primary WhatsApp path that already exists.

## Problem and why

The primary conversion is a WhatsApp conversation, but some visitors will not use WhatsApp. The contact form is today static markup with an inert button. It needs validation, protection against automated spam, server-side delivery to the photographer, and honest feedback to the visitor, without turning the static site into a server-rendered one.

## Scope

In scope:

- Pure domain logic with unit tests: validation and normalization of a submission, honeypot detection, plain-text email composition safe against header injection, and a use case that orchestrates captcha verification and delivery through injected ports.
- Server side: one on-demand endpoint (`POST /api/contact`) on Cloudflare through the Astro adapter, with adapters for Cloudflare Turnstile verification and Resend delivery over `fetch`, typed environment configuration, and safe behavior when delivery is not configured.
- Client side: the form markup completed (names, `autocomplete`, required marker, honeypot, Turnstile container, live status region), a script that loads Turnstile lazily, submits with `fetch`, shows Spanish success and error states, manages focus, and a `<noscript>` message that points to WhatsApp and email.
- Review of the WhatsApp entry points (hero, header, biography) for consistent deep links.

Out of scope: appointment scheduling, CRM or storage of enquiries, rate limiting beyond Turnstile and the honeypot (needs a KV or Durable Object binding), HTML email templates, analytics events and the deploy workflow (`production-delivery`), buying the domain and creating the Resend and Cloudflare accounts (user).

## Constraints and decisions

- The user has no Resend account yet; mail will be sent from a custom domain that will also host the site (decision on 2026-09-18). Everything is configuration-driven: `RESEND_API_KEY`, `CONTACT_FROM_EMAIL`, `CONTACT_TO_EMAIL`, `TURNSTILE_SECRET_KEY`, `PUBLIC_TURNSTILE_SITE_KEY`. Real end-to-end delivery is verified later, once the domain is verified in Resend.
- Development and tests use Cloudflare's official Turnstile test keys. When delivery is not configured the endpoint answers `503` with the code `not_configured` and the form tells the visitor to use WhatsApp or email; it never pretends to have sent anything.
- Hosting: `@astrojs/cloudflare@14` (Astro `^7.2`, Wrangler `^4.125`, verified 2026-09-18). The site stays `output: 'static'`; only `/api/contact` sets `export const prerender = false`. Secrets are optional in the `astro:env` schema so builds and CI never need them.
- `astro:env` fields with `access: 'public'` are inlined as literals at build time, even server-only ones, and a build without them tree-shakes the code that depends on them. Everything that must be configurable at deploy time is therefore `access: 'secret'` (`RESEND_API_KEY`, `TURNSTILE_SECRET_KEY`, `CONTACT_FROM_EMAIL`, `CONTACT_TO_EMAIL`), read from the Worker environment (secrets or `vars`, `.dev.vars` locally). `PUBLIC_TURNSTILE_SITE_KEY` is client-side and build-time: it has to exist in the environment that runs `astro build`, which `production-delivery` must provide.
- The adapter defaults add an Images binding and a KV namespace binding for sessions. The project uses neither, so `session: false` and `imageService: 'compile'` keep the generated Worker config free of resources to provision.
- Resend is called through its REST API with `fetch`; the `resend` SDK is not installed (it drags a React email peer and is unnecessary on Workers).
- Hexagonal boundary: `src/contact/` holds the domain and the use case with ports (`CaptchaVerifier`, `EmailSender`); `src/contact/adapters/` holds Turnstile and Resend; the endpoint file only wires them, because `astro:env/server` is a virtual module that unit tests cannot import.
- The domain speaks in error codes (`email_required`, `email_invalid`, `too_long`, `phone_invalid`), never in Spanish. Spanish messages live in `src/config/site.ts` and reach the client script through `data-*` attributes; client scripts do not import the site config.
- Required fields follow the mockup: only `E-mail`. Name, phone and message are optional and length-limited (names 80, phone 30, message 2000, email 254).
- A filled honeypot is answered as a success without sending anything, so bots learn nothing. The JSON key is `website` (server contract), but the DOM field is `topic_ref` with a neutral label, an `inert` and `aria-hidden` wrapper and the password-manager ignore attributes: a bait field named or labelled "website" is a documented autofill false positive, and a false positive here silently discards a real enquiry behind a success message. Do not rename it back.
- No personal data is logged. Responses never echo the submitted values.
- Without JavaScript the Turnstile widget cannot run, so the form cannot be submitted; a `<noscript>` block says so and offers WhatsApp and email.
- TDD: off (no project or session configuration enables it; the user did not request it). Source: default. Runner: Vitest (`pnpm test`). Tests accompany all pure logic and the adapters (with a fake `fetch`); the form is verified in a real browser.

## Delivery strategy

User rule: pull requests under 1000 authored lines, stacked when bigger; generated or mechanical diffs are valid exceptions. Each pull request gets an independent read-only review while the review bot is out of quota, and is reported ready only after CI is green and the review findings are fixed.

1. `feat/contact-domain` into `main`: CC-01. Types, the shared character sanitizer, validation, their tests and this document. No new dependencies, so it does not collide with the open media pull requests. The domain slice was first estimated at 500 to 650 lines; after the review hardening it measured 1394 with tests, so it was split here to respect the limit. About 950 authored lines.
2. `feat/contact-use-case` stacked on 1: CC-02 and CC-03. Email composition, the use case and their tests. About 560 authored lines.
3. `feat/contact-adapters` into `main` (1 and 2 are merged): CC-04 and CC-05. Adapter, environment schema, Wrangler config, Turnstile and Resend adapters. About 650 authored lines plus the lockfile. The slice was estimated at 450 to 600 lines in total and measured about 1500 with tests, so it was split here.
4. `feat/contact-endpoint` stacked on 3: CC-06. Request handler, route, README. About 900 authored lines.
5. `feat/contact-form-markup` into `main` (1 to 4 are merged): CC-07. Markup, Spanish copy, the error color token and the pure response-mapping helpers with their tests. About 850 authored lines. The slice was estimated at 500 to 650 lines and measured about 1450 with tests after the review, so it was split here.
6. `feat/contact-form` stacked on 5: CC-08 and CC-09. Client script and browser verification. About 620 authored lines.

## Tasks

- [x] CC-01 `src/contact/types.ts` and `src/contact/validate.ts`: submission shape, error codes, normalization (trim, collapse whitespace, lowercase email), limits, honeypot; unit tests with literal expectations.
- [x] CC-02 `src/contact/compose.ts`: subject, plain-text body and `replyTo` for the photographer, with CR and LF stripped from single-line fields; unit tests including injection attempts.
- [x] CC-03 `src/contact/handle.ts`: use case over injected ports returning a result union (`sent`, `invalid`, `spam`, `captcha_failed`, `not_configured`, `delivery_failed`); unit tests with fakes covering order of operations (no captcha call for invalid input, no send when the captcha fails).
- [x] CC-04 Cloudflare adapter, `wrangler.jsonc`, `astro:env` schema with optional secrets, `.dev.vars.example`; site stays static; CI build passes without secrets.
- [x] CC-05 `src/contact/adapters/turnstile.ts` and `src/contact/adapters/resend.ts` over `fetch`, with timeouts and no value echo; unit tests with a fake `fetch`.
- [x] CC-06 `src/pages/api/contact.ts`: JSON and form-encoded bodies, method and size guards, result to HTTP status mapping, no logging of personal data; verified locally against the Turnstile test keys.
- [x] CC-07 Form markup and Spanish copy: required marker, honeypot, Turnstile container, status region, `<noscript>` fallback.
- [x] CC-08 `src/scripts/contact-form.ts`: lazy Turnstile load, `fetch` submit, busy state, field errors tied to inputs with `aria-describedby`, focus to the first invalid field or to the status, success reset; pure mapping helpers unit-tested.
- [x] CC-09 Verify in a browser: invalid email, success path with the test keys, captcha failure with the always-fail test key, `not_configured`, keyboard-only flow, reduced motion, no console errors; WhatsApp links consistent.

## Acceptance criteria

- A visitor can submit the form with only an email and gets a clear Spanish confirmation; invalid input is explained next to the field and focus moves to it.
- An automated submission without a valid Turnstile token is rejected; a honeypot hit is swallowed silently.
- With delivery not configured, the visitor is told to use WhatsApp or email and nothing is reported as sent.
- The homepage remains prerendered and ships no server code; only `/api/contact` runs on demand.
- `pnpm check` passes without any secret; all pure logic and adapters have unit tests that fail under mutation.
- No submitted value is logged or echoed.

## Progress and evidence

- 2026-09-18 CC-01 to CC-03 written by one delegated writer (Sonnet): `src/contact/{types,validate,compose,handle}.ts` with tests, no new dependencies, no existing file touched. First pass: 62 tests, `pnpm check` exit 0, five mutations caught (honeypot checked too late, sending after a failed captcha, no CR and LF stripping, a limit off by one, no email lowercasing). Design choice accepted: error precedence is required, then `too_long`, then format.
- 2026-09-18 Independent review before any pull request (the review bot is out of quota). The reviewer ran the tests and probed the modules with scratch scripts. Confirmed sound: no prototype pollution from JSON bodies, arrays and non-string values handled, Unicode spaces collapsed, no value echo, no logging, surrogate pairs preserved, no catastrophic backtracking (largest probe linear), and `\n{3,}` versus `\n{2,}` is an equivalent mutant. Findings, all fixed by the writer: (1) the email check accepted values that are not one bare address (`"x"<a@b.com>`, `<a@b.com>`, `a@b.com,evilrecipient.com`, IP literals, dots at the edges of the local part, hyphen-edged labels), which matters because the email becomes `replyTo`; it is now a strict allowlist (local part 1 to 64 from a fixed charset with dot rules, domain labels 1 to 63 of `a-z 0-9 -` without edge hyphens, at least two labels, alphabetic last label of two or more); non-ASCII addresses are rejected on purpose and punycode domains are accepted; (2) only code points below 0x20 were stripped, so DEL, C1 controls including NEL, U+2028 and U+2029, zero-width marks, bidi embeddings, overrides and isolates, the word joiner range and the BOM survived, and a right-to-left override in a name reversed the rendered subject; the new shared `src/contact/sanitize.ts` removes them in validation and again in composition; decision: the zero-width joiner is stripped from single-line fields and kept in the message body so family emoji are not broken, bidi controls are stripped everywhere; (3) the captcha result is compared with `=== true`, because `"false"`, `1`, `{}` and `[]` were treated as success; (4) two mutation blind spots closed: an invalid payload on an unconfigured site reports the field errors (decision now explicit: `not_configured` only for an otherwise valid submission), and non-boolean verifier results give `captcha_failed` with no send; (5) a throwing getter no longer breaks the "never throws" contract; (6) honeypot table for non-string values; (7) the message limit is documented as UTF-16 code units.
- Tooling hazard found by the writer: `\uXXXX` escapes typed through the editing tools were decoded into raw characters on disk, so test files briefly contained real invisible and bidi characters. They were rewritten with `String.fromCodePoint(0x...)` constants. Parent byte scan of `src`, `scripts`, `studio` and the config files for C1 controls, zero-width marks, bidi controls, separators and the BOM: no match.
- Evidence after the fixes: `pnpm test` 8 files and 175 tests (120 under `src/contact`); `pnpm lint`, `pnpm format:check`, `pnpm typecheck` (45 files, 0 errors) and `pnpm check` exit 0. Mutations caught: `not_configured` before validation (1 test), `!captchaOk` instead of `!== true` (3), sanitizer keeping U+202E (4 across three files), a leading dot in the local part (1), and the sanitizer removed from `compose.ts` only (4, which proves composition is safe without validation). Parent probe: real addresses validate and normalize (`Fernando@Nunez.com.co` becomes lowercase), every malformed form above is rejected, and a payload of several megabytes with a million overrides is handled in 437 ms. Native risk assessment `medium`.
- 2026-09-18 Review bot comment on the first pull request (P2, valid): U+061C ARABIC LETTER MARK, part of Unicode's `Bidi_Control` set, survived both sanitizer modes. Verified with a probe script, which also showed the deprecated format controls U+206A to U+206F, the interlinear annotation controls U+FFF9 to U+FFFB, the Mongolian vowel separator U+180E and the soft hyphen U+00AD surviving. All are now stripped in both modes; U+2060 to U+206F is one block. Twelve new table rows fail against the previous sanitizer and pass with the fix; three more pin what must be kept (Arabic text around the mark, the neighbours U+205F and U+2070, and the visible U+FFFD). Deliberately still kept: variation selectors and tag characters (emoji presentation and subdivision flags), the combining grapheme joiner and the Hangul fillers (letters, not format controls).
- 2026-09-18 CC-04 and CC-05 by the same delegated writer on top of the merged domain. `@astrojs/cloudflare` 14.3 with Wrangler 4.135; `output` stays `static` and the adapter prerenders inside workerd, so the two `process.env` fallbacks are guarded with `typeof process`. Verified by the parent: `pnpm check` exit 0 with no contact variable set; nine portfolio links, five fonts, no `/_image` and no endpoint reference in the prerendered homepage; the generated `dist/server/wrangler.json` serves only `../client` as assets and has no Images and no KV binding; the lockfile rebased onto the media modules installs frozen and without drift. In a checkout without `.env` (the CI conditions) `CONTENT_FALLBACKS=true` from the shell is honored during workerd prerendering and a strict build with a broken project id fails; locally the `.env` file wins over shell variables under the adapter.
- Adapters: the Turnstile verifier resolves `true` only for a 2xx response whose JSON has `success === true` and never throws; the Resend sender rejects with the HTTP status, `timeout` or `network` only and never reads the response body. Mutations caught: `Boolean(json.success)`, the response body in the error message, and a hardcoded timeout in either adapter (the configured value is asserted on `AbortSignal.timeout`).
- 2026-09-18 CC-06: `src/contact/http.ts` is a framework-independent handler over `Request` and `Response`; `src/pages/api/contact.ts` only wires the ports from `astro:env/server`. Guards in order: method (`405` with `Allow`), same origin when an `Origin` header is present (`403`), declared and counted body size (`413`, 32 KiB), content type (`415`), body shape (`400`, arrays included). `sent` and `spam` share one byte-identical `200`; `invalid` is `422` with codes only; `captcha_failed` `400`, `not_configured` `503`, `delivery_failed` `502`; every response is `no-store` and `nosniff`; nothing is logged or echoed.
- Independent review before the pull requests, run against the BUILT worker (`wrangler dev --local --config dist/server/wrangler.json`, which reads `.dev.vars` next to that generated file) with the Turnstile test keys: `503` unconfigured, `400` with the always-fail secret, `502` when the request reaches Resend with a fake key. No blockers. Confirmed sound: secrets are available at module scope through `cloudflare:workers`, no prototype pollution through any of the three body types (20,000 keys in under 15 ms), the origin check against case, default ports and paths, no logging and no echo. Fixed: the two addresses were `access: 'public'` and therefore frozen at build time; the unused adapter bindings; the body is now read once with a byte bound instead of a cloned second pass; JSON arrays are rejected; four test blind spots closed (exact size boundary, `cf-turnstile-response` in JSON bodies, the timeout value, scheme and port in the origin check). Seven mutations caught after the fixes; the parent re-ran the size boundary one. Declined: a `Sec-Fetch-Site` check (no cookies or ambient authority; Turnstile is the gate). A lying small `Content-Length` is framed by the HTTP server before the handler runs and ends as `400`; the counted read protects chunked bodies.
- Evidence: `pnpm check` exit 0 with no contact variable set, 14 files and 274 tests, typecheck 0 errors. Native risk assessment `medium`.
- 2026-09-18 CC-07 by a fresh delegated writer: the form is safe without JavaScript (no `action`, the submit button is rendered `disabled` and the script enables it, a `<noscript>` block links to WhatsApp and e-mail), fields carry the domain limits as `maxlength`, each has an error container tied with `aria-describedby` only while it has text, and two live regions (`role="status"` and `role="alert"`) are always rendered and visually hidden while empty, because a region that is `display: none` until its text arrives is not announced reliably. Form-level messages never move focus; only a field error and the interactive challenge do. Spanish copy lives in `src/config/site.ts`, one message per domain error code, enforced by a test. `src/lib/contact-form-state.ts` holds the pure parts: the payload builder, `interpretResponse` as a closed union (an unknown field or code degrades to a form-level error, never to success), the focus order, the message mapping and the classification of a failure as captcha or network.
- Independent review before the pull requests, in a real browser. Blockers fixed: the honeypot naming above, and a cached rejected promise that made one blocked Turnstile script load break the form until a reload. Also fixed: copy that told the visitor to refresh the page (it destroys what they typed) and a phone message that did not match the rule; `--color-text-muted` moved from `#7a7a7a` (4.29:1) to `#767676` (4.54:1) because the new small text failed AA; the message mapping lived untested in the DOM script and survived a swap mutation with 300 green tests, so it moved into the pure module. Mutations caught on the pure helpers: ten by the writer and the reviewer plus the parent's swap re-run. Declined: an always-visible hint for the case where the site's own script fails to load.
- 2026-09-18 CC-08: `src/scripts/contact-form.ts` enables the button, validates with the same `validateContactSubmission` the server uses (no network call for an invalid form), loads Turnstile lazily on the first `focusin` or when the form nears the viewport, renders it with `execution: 'execute'` and `appearance: 'interaction-only'`, executes on submit, posts JSON with a timeout and resets the widget after every attempt. A filled honeypot follows the exact same path, so the client behaves identically either way. Busy state uses `aria-disabled`, never `disabled`, so focus is not lost. With no site key at build time Turnstile is never loaded and a valid submit shows the `not_configured` message. A failed script load clears its cache and retries on the next attempt. When Turnstile needs interaction, `before-interactive-callback` stops our own 15 second countdown (Turnstile's `timeout-callback` takes over), shows a Spanish instruction and moves focus next to the challenge; `error-callback` returns `true` so Turnstile does not log to the console.
- CC-09 in a real browser with the Turnstile test keys (`1x…AA` passes, `2x…AB` blocks, `3x…FF` forces interaction): empty and invalid e-mail stay local with focus on the field; a routed `422` maps to the right field; `503`, `400` with the always-fail secret and `502` with a fake Resend key show their messages against the real server; success (routed `200`, because no Resend account exists yet) resets the form; a double click sends one request; the keyboard-only flow works; the Turnstile script is not requested on load and is requested once after the first focus; a blocked script recovers after unblocking; the interactive challenge is no longer cut off at 15 seconds; both live regions are in the accessibility tree while empty and leave zero residual height; no horizontal scroll at 393 and 1366; no console output of ours; without JavaScript the button is disabled and the `<noscript>` links are visible; every WhatsApp entry point produces the same `wa.me` link. Client chunk: 9 KB raw.
- 2026-09-18 Review bot on the behavior pull request, two P2 findings, both valid and fixed. (1) The validator checks the bait first, so a filled bait plus an empty e-mail skipped the local field error and called the server while the same form without the bait stopped locally; local validation now runs on `fieldsForLocalValidation(values)` (bait forced empty, pure and tested) and the raw bait still travels in the payload, so the client path is identical with and without it. The server still checks the bait first by design. (2) A Turnstile script request that hangs without `load` or `error` left the cached promises pending, so every later submit waited ten seconds again; the loader now evicts itself on a timer, reuses an already loaded `window.turnstile` instead of loading `api.js` twice, and ignores late events from an evicted element. Verified in the browser with a route that never settles: captcha message at about ten seconds, then a fresh load and exactly one `api.js` element after recovery. Two more mutations caught. 313 tests.
- Evidence: `pnpm check` exit 0 with no contact variable set, 15 files and 313 tests, typecheck 0 errors; the built homepage has no `data-sitekey` and no Turnstile reference. Native risk assessment `medium`.
- Known limits: message length is counted in UTF-16 code units, so an emoji counts as two; addresses with non-ASCII characters are rejected; no rate limiting beyond Turnstile and the honeypot; if the site's own script fails to load, the button stays disabled without a hint (the e-mail and phone links sit beside the form on desktop only); a real `sent` has not been exercised end to end because no Resend account exists yet.

## Next step

The module is complete once both form pull requests merge. Then `production-delivery`, which must provide `PUBLIC_TURNSTILE_SITE_KEY` to the build and the four runtime values to the Worker, and must exercise one real delivery once the domain is verified in Resend. The user adds the contact variables to `.env.example` (the path is write-protected for the agents). `production-delivery` must provide `PUBLIC_TURNSTILE_SITE_KEY` to the build and the four runtime values to the Worker.
