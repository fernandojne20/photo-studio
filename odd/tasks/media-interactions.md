# Feature: media-interactions

Module ID: `media-interactions` (see `CAPABILITY-MAP.md`). Depends on `studio-page`. Can run in parallel with `contact-conversion`; both feed `production-delivery`.

## Objective

Provide the portfolio lightbox, the service carousel, the hover zoom, and their touch, keyboard, focus and reduced-motion behavior, as enhancements over content that stays usable when client-side JavaScript fails.

## Problem and why

The homepage shows the portfolio grid and the service cards as static images. A prospective client needs to inspect photographs at full size comfortably on phone and desktop, and the service row must keep working when the photographer adds more than four services.

## Scope

In scope:

- Lightbox for the portfolio grid: open from any thumbnail, previous and next, keyboard (arrows, Escape), swipe and pinch zoom on touch, accessible dismissal with focus returned to the trigger, optional captions, Spanish control labels, reduced-motion behavior, full-size responsive images from the Sanity CDN.
- Progressive enhancement: each thumbnail is a plain link to the full image, so the photo still opens without JavaScript.
- Service carousel on desktop when the cards exceed the available width (about four cards at the design width), with previous and next controls, keyboard support and reduced-motion behavior. Mobile keeps the stacked layout.
- Restrained hover zoom on portfolio thumbnails and service cards for hover-capable, fine-pointer devices only, disabled under reduced motion.
- Unit tests for the pure logic; browser verification of the interactions.

Out of scope: category pages or filtering, links on service cards, video, deep links to a specific photo, analytics events (`production-delivery`).

## Constraints and decisions

- Lightbox library: PhotoSwipe 5 (`photoswipe@5.4.4`, MIT, no dependencies; verified from the published package on 2026-09-18: loader 4.5 KB gzip loaded with the page, core 16.4 KB gzip loaded on first open through a dynamic import, CSS 2.4 KB gzip). Chosen over a custom `<dialog>` lightbox because swipe, pinch zoom, preloading and focus management are where hand-rolled lightboxes fail, and pinch zoom matters on a photography site. Tradeoff accepted: the last release is from May 2024. The user was offered both options and asked to continue without choosing, so the recommendation proceeds and is called out in the pull request.
- Carousel library: Embla (`embla-carousel@8.6.0`, MIT, no dependencies), fixed by the intent document.
- The lightbox shows the image as the photographer framed it in the Studio: the Studio `crop` applies, the 4:5 thumbnail crop does not. Full-image dimensions are computed from the asset dimensions and the crop fractions; candidates never exceed the available width. Thumbnails and full images have different ratios, so links carry `data-cropped="true"` for a correct opening animation.
- Captions come from the CMS `caption`; they are rendered inside the link as visually hidden text so they exist without the lightbox, and shown in the lightbox through PhotoSwipe's `uiRegister` API. No extra caption plugin.
- Client scripts do not import `src/config/site.ts` (that would ship the whole copy to the browser). Labels reach the script through `data-*` attributes rendered by the component.
- Reduced motion: no open, close or zoom animation in the lightbox, no carousel easing, no hover zoom.
- Styling through tokens only; PhotoSwipe is themed through its CSS variables.
- Browser scripts are linted with browser globals only and `no-undef` on (see `eslint.config.mjs`).
- TDD: off by default. Tests accompany the pure logic; interactions are verified in a real browser with Playwright.

## Delivery strategy

User rule: pull requests under 1000 authored lines, stacked when bigger; generated or mechanical diffs (lockfile) are valid exceptions. Planned before writing:

1. `feat/media-lightbox` into `main`: tasks MI-01 to MI-04. Estimated 450 to 600 authored lines including tests.
2. `feat/media-carousel` stacked on 1: tasks MI-05 to MI-07. Estimated 350 to 500 authored lines. Stacked rather than parallel because both add a dependency and would conflict in `pnpm-lock.yaml`.

Each pull request is reported as ready only after CI is green and the review bot has had about ten minutes to respond.

## Tasks

- [x] MI-01 Pure lightbox data: `src/lib/lightbox-item.ts` builds the full-image `href`, `srcset`, width and height for a `CmsImage` (crop-aware, no upscaling, placeholders pass through), with unit tests.
- [x] MI-02 Grid markup: thumbnails become links with `data-pswp-*` attributes, `data-cropped`, hidden captions, keyboard focus styles; the page still works with JavaScript disabled.
- [x] MI-03 Lightbox behavior: `src/scripts/lightbox.ts` initializes PhotoSwipe lazily with Spanish labels from `data-*` attributes, the caption element, reduced-motion options and token-based theming.
- [x] MI-04 Verify the lightbox: unit tests, `pnpm check`, and Playwright on the production build: open, next with the keyboard, caption visible for a captioned item, Escape closes and focus returns to the trigger, the core chunk loads only on first open, the link works with JavaScript disabled.
- [x] MI-05 Service carousel with Embla on desktop when cards overflow; stacked layout unchanged on mobile; controls, keyboard support, reduced motion; pure logic unit-tested.
- [x] MI-06 Hover zoom on portfolio thumbnails and service cards, gated by `(hover: hover) and (pointer: fine)` and reduced motion.
- [x] MI-07 Verify the carousel and hover zoom: `pnpm check`, Playwright with four services (static row) and with more than four (carousel active), mobile unaffected.

## Acceptance criteria

- A visitor can open any portfolio photo, move through all photos with arrows, swipe or keys, read the caption when one exists, and close with Escape, the close button or a tap outside, landing back on the thumbnail they opened.
- With JavaScript disabled, clicking a thumbnail opens the full photo.
- With reduced motion requested, nothing animates.
- The page ships no lightbox core code until a photo is opened.
- With more than four visible services the desktop row scrolls as a carousel with reachable controls; with four or fewer it is a static row; mobile stays stacked.
- All checks in `pnpm check` pass; new pure logic has unit tests that fail under mutation.

## Progress and evidence

- 2026-09-18 MI-01 to MI-03 done by one delegated writer (Sonnet) on `feat/media-lightbox`. New: `src/lib/lightbox-item.ts` (pure, crop-aware, type-guarded, no `any`) with tests, `src/scripts/lightbox.ts` (lazy PhotoSwipe init, labels from `data-*`, text-only caption through `uiRegister`, explicit reduced-motion options, focus trap and return, Escape and arrow keys). Modified: `PortfolioGrid.astro` (links with `data-pswp-width`, `data-pswp-height`, `data-pswp-srcset`, `data-cropped`, hidden captions, focus style, `.pswp` theming through tokens), `site.ts` (six Spanish lightbox labels), `placeholders.ts` (a caption on the first placeholder for parity when developing without the CMS), `package.json` and lockfile (`photoswipe@5.4.4`). All PhotoSwipe option names matched the installed types. Writer evidence: `pnpm test` 5 files and 63 tests; `pnpm lint`, `pnpm format:check`, `pnpm typecheck` (39 files, 0 errors) and `pnpm check` exit 0. The writer found that the crop example in the brief preserved the aspect ratio and could not detect an ignored crop, and added a second case (crop left and right 0.4 gives a single `800w` candidate); mutations "ignore the crop" and "drop the no-upscale filter" both fail tests and were reverted. Build: the PhotoSwipe core is its own chunk (`photoswipe.esm.*.js`, about 59 KB raw) and `dist/index.html` references only the small entry script, with no `modulepreload` for the core. Browser on the production preview, 1366x900 and 393x800: no core request before the first click; after clicking the first thumbnail the lightbox opens with the counter `1 de 9`, the close title `Cerrar` and the caption "Sesión en exteriores"; ArrowRight gives `2 de 9` with an empty hidden caption; Escape closes and `document.activeElement` is the first `portfolio__link`; 0 console errors or warnings.
- 2026-09-18 MI-04 and parent fixes. Native risk assessment `medium` (configuration change in `package.json`), satisfied by writer self-verification plus parent readback of `lightbox.ts` and `lightbox-item.ts` and a spot check (`pnpm test` 63 passed, `pnpm lint` clean). Readback found a real flaw in the sizing rule: the condition guarding the "offer the full width" case could never be true, so an image between two steps (2000 px wide) was capped at 1600. Fixed so the displayed width is offered whenever it is below the 2400 maximum and not already a candidate; new test: a 2000x1500 asset gives `1200w`, `1600w`, `2000w` and 2000x1500. No-JavaScript path verified on the built page: the first link's `href` is a Sanity CDN image that returns `200 image/jpeg`; 9 links, all with `target="_blank"`, 3 hidden captions matching the three seeded captions.
- 2026-09-18 Independent review of pull request #7. The review bot is out of quota (it answers with a usage-limit message as an issue comment), so a read-only reviewer went over the diff through git plumbing and traced the installed `photoswipe` and `@sanity/image-url` sources. Confirmed correct: the Studio crop reaches `urlFor` and the library applies `rect: source.crop` for width-only requests, so computed and delivered dimensions agree. Findings, each verified and fixed by the lightbox writer: (1) real bug: `showAnimationDuration`, `hideAnimationDuration` and `zoomAnimationDuration` were passed as `undefined` when motion was not reduced, and PhotoSwipe merges `{ ...defaultOptions, ...options }`, so regular users lost every animation and `--pswp-transition-duration` became `undefinedms`; options now come from the pure `src/lib/lightbox-options.ts`, which never emits an `undefined` key, with unit tests; (2) `buildLightboxItem` now ignores unusable crops (non-finite, negative, or summing to 1 or more on an axis) and passes through when the base dimensions are not finite and positive, instead of dividing by zero or requesting `w=0`; (3) the crop tests now distinguish the axes (crop bottom 0.5 gives `2400 x 1125`) and assert both dimensions; (4) `src/scripts/**` is linted with browser globals and `no-undef`, as this document already claimed; (5) the page behind the open lightbox is made `inert` on `afterInit` and restored on `close`; (6) `photoswipe` uses a caret range like every other dependency. Discovery while fixing (5): PhotoSwipe only returns focus for keyboard-initiated opens, and its focus trap is still bound during the `close` dispatch, so the script refocuses the trigger in a microtask.
- Evidence after the fixes: `pnpm test` 6 files and 76 tests; `pnpm lint`, `pnpm format:check`, `pnpm typecheck` (41 files, 0 errors), `pnpm check` exit 0. Mutations caught: removing the non-negative guard (after the writer noticed its own first fixture, -0.1 and 0.1, cancelled out and replaced it), swapping the crop axes, a `process.env` read in `src/scripts/lightbox.ts` (fails `no-undef`). Browser on the production build: with default motion the computed `--pswp-transition-duration` is `333ms`; while open `main`, `header` and `footer` are inert and `.pswp` is not; after Escape all three are restored and `document.activeElement` is the first `portfolio__link`; with `page.emulateMedia({ reducedMotion: 'reduce' })` the variable is `0ms` and open, close and focus return still work; no console errors. Native risk assessment `medium` (executable change in `eslint.config.mjs`); parent readback of `lightbox-options.ts` and the background lock.
- Known limits: swipe and pinch were not exercised by automation and rely on PhotoSwipe's own behavior. Safari does not focus a link on click, so after a mouse-opened lightbox closes there is no trigger to refocus there; keyboard-opened lightboxes return focus everywhere.

- 2026-09-18 Pull request #7 (`feat/media-lightbox` into `main`) opened with all four CI jobs green. The review bot answered with a usage-limit message (as an issue comment, which the first watcher did not poll), also after an explicit `@codex review`, so bot reviews are unavailable for now. Compensation: an independent read-only reviewer on the pull request diff through git plumbing, and the watcher now polls reviews, reactions and bot comments.
- 2026-09-18 MI-05 and MI-06 done by one delegated writer (Sonnet) on `feat/media-carousel`, stacked on the lightbox branch. New: `src/lib/service-carousel.ts` (`shouldUseCarousel`, strictly more than four) with tests, `ServiceCardItem.astro` (shared card markup and its hover zoom rule), `ServicesCarousel.astro` (labelled region, scroll-snap fallback without JavaScript, buttons rendered `hidden` and revealed by the script), `src/scripts/service-carousel.ts` (Embla with `align: 'start'`, `containScroll: 'trimSnaps'`, inactive under 900px through `breakpoints`, `duration: 0` under reduced motion, button state from `canScrollPrev` and `canScrollNext`, ArrowLeft and ArrowRight on the region). Modified: `Services.astro` (delegates when the helper says so), `PortfolioGrid.astro` (zoom on hover and on `:focus-visible` of the link), `site.ts` (three Spanish labels), `package.json` and lockfile (`embla-carousel@8.6.0`), `eslint.config.mjs` (`no-noninteractive-tabindex` allows `role="region"`, with the reason). Writer findings worth keeping: Embla's option merge copies keys whose value is `undefined`, so `duration` is only set when needed; an author `display` rule on the buttons beat the user-agent `[hidden]` rule, so `.carousel__button[hidden] { display: none }` was added after the browser check caught visible buttons on mobile. Writer evidence: `pnpm test` 6 files and 68 tests; `pnpm lint`, `pnpm format:check`, `pnpm typecheck` (44 files, 0 errors) and `pnpm check` exit 0; mutation `>` to `>=` fails two tests. Browser with a temporary page rendering four and six fixture services: four gives no region and no buttons; six gives the region with previous disabled at the start, next moving the track and enabling previous, ArrowRight scrolling from the focused region, next disabled at the end; at 393px both are stacked with no transform and no visible buttons; the real homepage with four services references no Embla chunk; hover on the first thumbnail changes the image transform from `none` to `matrix(1.04, 0, 0, 1.04, 0, 0)` and the built rule sits inside `(hover:hover) and (pointer:fine) and (prefers-reduced-motion:no-preference)`; no console errors; the temporary page was deleted.
- 2026-09-18 MI-07 and parent changes. Native risk assessment `medium` (executable change in `eslint.config.mjs`), satisfied by writer self-verification plus parent readback of the script, the carousel markup and the lint rule change. Parent tweak: on mobile the wrapper no longer announces a carousel or adds a tab stop that scrolls nothing; the script removes `tabindex` and `aria-roledescription` under 900px and restores them above it. Verified live with a temporary six-service page: at 1366px the region has `tabindex="0"` and `aria-roledescription="carousel"`, previous is disabled and becomes enabled after next; after resizing to 393px both attributes are gone, the buttons compute `display: none`, and the container is a column with no transform. Temporary page deleted, rebuild confirms a single page and no `dist/carousel-check`.
- 2026-09-18 Independent review of pull request #8 (stacked, opened after a conflict-free rebase on the reviewed lightbox branch; `embla-carousel` moved to a caret range). The reviewer traced the installed Embla and `eslint-plugin-jsx-a11y-x` sources. Confirmed correct: no option is passed as `undefined`, `duration: 0` is Embla's own instant-scroll value, Embla clears its inline transform when it deactivates under 900px and emits `reInit` on every crossing and on resizes, the hover zoom is clipped with the right radius and cannot shift layout, and the static four-card path is layout-equivalent after the `ServiceCardItem` refactor. No blockers; findings fixed by the carousel writer: (1) progressive enhancement ran backwards: the server rendered `tabindex` and `aria-roledescription` and only the script removed them on mobile, leaving a mislabelled tab stop without JavaScript and, on desktop without JavaScript, focus on an element that does not scroll; the server now renders only `role="region"` and the label, and the script adds the carousel semantics while Embla is active and removes them when it is not; (2) the `no-noninteractive-tabindex` override replaced the preset's options (flat config does not merge rule options), dropping `tabpanel` and `allowExpressionValues`; with no static `tabindex` left in the markup it was reverted, and `eslint.config.mjs` is identical to the lightbox branch; (3) a button that became `disabled` while focused threw focus to `<body>`; buttons now use `aria-disabled` with guarded click handlers; (4) the arrow-key handler ignores modified keys (Alt+ArrowLeft is browser back) and does nothing while Embla is inactive; (5) the redundant `matchMedia` change listener is gone; (6) the controls row reserves `min-height` on desktop so revealing the buttons does not shift the content below; (7) the next label is "Servicios siguientes". Slides keep their list item role on purpose.
- Evidence after the fixes: `pnpm test` 7 files and 80 tests; `pnpm lint`, `pnpm format:check`, `pnpm typecheck` (46 files, 0 errors), `pnpm check` exit 0. Browser with a temporary six-service page (deleted afterwards): the built HTML has `role="region"` and the label but no `tabindex`, no `aria-roledescription`, and the previous button has `aria-disabled="true"` and no `disabled`; at 1366px the script adds both attributes; after clicking next to the end `document.activeElement` is still the next button and a further click leaves the transform unchanged; Alt+ArrowRight leaves the transform unchanged with `defaultPrevented` false while a plain ArrowRight scrolls; `.carousel__controls` computes `min-height: 44px` and a marker below keeps the same `top` while the buttons toggle; at 393px both attributes are gone, the buttons compute `display: none`, the container has no transform and ArrowRight is not prevented; back at 1366px everything is restored; no console errors; the real homepage still references no Embla chunk. Native risk assessment `medium`; parent readback of the script diff and confirmation that `PortfolioGrid.astro` changed only by collapsing a CSS comment to one line.
- Known limits: the carousel was exercised with fixture data because the dataset has exactly four services; drag and touch behavior relies on Embla and was not automated; resizing the window across 900px while a carousel button holds focus hides that button and can drop focus, an edge case left as is.

## Next step

The carousel branch was rebased on the reviewed lightbox branch (no conflicts), `embla-carousel` moved to a caret range, and `pnpm check` passes with 80 tests. Open the stacked pull request, cover it with an independent review while the bot is out of quota, and after both merge continue with `contact-conversion`.
